name: ci & cd

on:
  push:
    branches:
      - main
      - master

jobs:
  ci:
    runs-on: self-hosted
    steps:
      - name: Checkout 🛎
        uses: actions/checkout@master

      - name: Setup node env 🏗
        uses: actions/setup-node@v2.1.5
        with:
          node-version: ${{ matrix.node }}
          check-latest: true

      - name: Cache node_modules 📦
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - name: Install dependencies 👨🏻‍💻
        run: |
            node -v
            npm -v
            npm ci
      - name: Run tests 🧪
        run:  |
            node -v
            npm -v
            npm run test
      - name: Run build
        run:  |
            node -v
            npm -v
            npm run build

      - name: Install PM2
        run : npm i pm2

      - name: Run start
        run:  |
            node -v
            npm -v
<<<<<<< HEAD
            pm2 start ./middleware-server/redirectServer.js --name "redirectServer"
          #  sh -x start.sh
            pm2 list
        env:
          CI : true
          # pm2 reload echosystem.config.js --watch
=======
            pm2 reload echosystem.config.js            
            pm2 list
        env:
          CI : true
          #  pm2 reload echosystem.config.js --watch
>>>>>>> 67014f77515799a3c5b6578d71ca6241ba616931
          # sh start.sh