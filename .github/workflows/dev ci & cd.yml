name: dev ci & cd

on:
  push:
    branches:
      - dev

jobs:
  ci:
    runs-on: self-hosted
    steps:
      - name: Checkout 🛎
        uses: actions/checkout@master
        with:
          ref: dev
          path: cocomon_dev